# 🔑 Настройка SSH-ключей для автоматического входа на сервер

Эта настройка позволит подключаться к серверу без ввода пароля каждый раз.

---

## 🎯 Способ 1: Автоматический через скрипт (РЕКОМЕНДУЕТСЯ)

Я создал скрипт, который всё сделает за вас!

---

## 📋 Способ 2: Ручная настройка

### Шаг 1: Создайте SSH ключ на Windows

Откройте PowerShell и выполните:

```powershell
# Генерируем SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"
```

**Когда спросит:**
- **"Enter file in which to save the key"** → просто нажмите `Enter` (будет использован путь по умолчанию)
- **"Enter passphrase"** → можете оставить пустым (просто нажмите `Enter` дважды)

### Шаг 2: Скопируйте ключ на сервер

```powershell
# Замените на ваш IP
ssh-copy-id root@45.132.19.34
```

Введите пароль **один раз**, после этого пароль больше не понадобится!

**Если команда `ssh-copy-id` не работает**, используйте этот способ:

```powershell
# Показываем содержимое ключа
type $env:USERPROFILE\.ssh\id_ed25519.pub

# Скопируйте весь вывод (он начнется с ssh-ed25519)
# Затем подключитесь к серверу:
ssh root@45.132.19.34

# На сервере выполните:
mkdir -p ~/.ssh
echo "ВСТАВЬТЕ_СКОПИРОВАННЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
exit
```

### Шаг 3: Проверьте

```powershell
ssh root@45.132.19.34
```

Если подключилось без пароля - всё готово! ✅

---

## 🔧 Способ 3: Через WinSCP (самый простой для Windows)

### 1. Откройте WinSCP

### 2. Настройте подключение:
- **Хост:** `45.132.19.34`
- **Пользователь:** `root`
- **Пароль:** ваш пароль

### 3. Подключитесь к серверу

### 4. В меню выберите:
**"Options" → "Preferences" → "Authentication"**

### 5. Нажмите **"Generate or Import Key"**

### 6. Сгенерируйте новый ключ:
- Нажмите **"Generate"**
- Нажмите **"Save public key"** → сохраните как `id_ed25519.pub`
- Нажмите **"Save private key"** → сохраните где удобно (например, в `C:\Users\uri3l\.ssh\`)

### 7. Загрузите публичный ключ на сервер:
- В WinSCP подключитесь к серверу
- Перетащите файл `id_ed25519.pub` в папку `/root/.ssh/` на сервере

### 8. Откройте терминал в WinSCP и выполните:

```bash
cd ~/.ssh
cat id_ed25519.pub >> authorized_keys
chmod 600 authorized_keys
```

### 9. Настройте WinSCP для использования ключа:
- **Options** → **Preferences** → **Authentication**
- В поле **"Private key file"** укажите путь к сохраненному приватному ключу
- Снимите галочку **"Password"**
- Попробуйте подключиться снова

---

## ✅ Проверка

После настройки любого из способов:

```powershell
ssh root@45.132.19.34
```

Если подключилось **без запроса пароля** - всё работает! 🎉

Теперь скрипт `quick-update-git.bat` будет работать без ввода пароля.

---

## ❓ Если не работает

### Проблема: "Permission denied"

**Решение:** Проверьте права на файлы на сервере:
```bash
ssh root@45.132.19.34
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### Проблема: Ключ не добавляется

**Решение:** Добавьте вручную через SSH:
```bash
ssh root@45.132.19.34
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# Вставьте содержимое вашего id_ed25519.pub файла
# Сохраните: Ctrl+O, Enter, Ctrl+X
chmod 600 ~/.ssh/authorized_keys
```

---

## 💡 Где находятся ключи в Windows?

- **Публичный ключ:** `C:\Users\uri3l\.ssh\id_ed25519.pub`
- **Приватный ключ:** `C:\Users\uri3l\.ssh\id_ed25519`

Эти файлы нужны для доступа - не удаляйте их!

