# 🔧 Установка Git для работы с GitHub

## ❌ Проблема

Вы получили ошибку:
```
git : Имя "git" не распознано как имя командлета, функции, файла сценария или выполняемой программы.
```

Это означает, что Git не установлен на вашем компьютере.

---

## ✅ Решение: Установка Git

### Способ 1: Скачать с официального сайта (рекомендуется)

1. **Зайдите на https://git-scm.com/download/win**
2. **Скачайте Git для Windows** (автоматически определится 64-bit версия)
3. **Запустите установщик** и нажимайте "Next" везде (настройки по умолчанию подходят)
4. **Перезапустите PowerShell** после установки

### Способ 2: Через winget (если установлен)

Откройте PowerShell как администратор и выполните:
```powershell
winget install --id Git.Git -e --source winget
```

### Способ 3: Через Chocolatey (если установлен)

```powershell
choco install git
```

---

## 🔍 Проверка установки

После установки откройте **новое окно PowerShell** и проверьте:

```powershell
git --version
```

Должно показать что-то вроде:
```
git version 2.42.0.windows.2
```

---

## 🚀 После установки Git

Теперь можете выполнить команды из предыдущей инструкции:

```powershell
# Перейдите в папку проекта
cd C:\Users\uri3l\Perps-info-tg-bot

# Инициализируйте Git
git init

# Добавьте файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Подключите ваш репозиторий (ЗАМЕНИТЕ ссылку!)
git remote add origin https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git
git branch -M main
git push -u origin main
```

---

## 🎯 Альтернатива: Использовать простой способ

Если не хотите устанавливать Git, используйте **`quick-update.bat`**:

1. Откройте `quick-update.bat` в блокноте
2. Измените IP адрес на строке 18:
   ```batch
   set SERVER=ваш_IP_адрес
   ```
3. Сохраните и запускайте двойным кликом после изменений

**Этот способ не требует Git!**

---

## 📋 Что выбрать?

| Способ | Нужен Git | Преимущества |
|--------|-----------|--------------|
| **quick-update.bat** | ❌ Нет | Простота, быстрый старт |
| **quick-update-git.bat** | ✅ Да | История изменений, откат ошибок |

---

## 💡 Рекомендация

**Для начала:** Установите Git и используйте GitHub версию - это удобнее в долгосрочной перспективе.

**Если не хотите:** Используйте `quick-update.bat` - он работает без Git.


