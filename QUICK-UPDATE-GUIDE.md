# 🚀 Быстрое обновление бота на сервере

После изменений в коде используйте один из этих способов:

---

## ✅ Способ 1: Через .bat файл (САМЫЙ ПРОСТОЙ - РЕКОМЕНДУЕТСЯ)

### 📝 Первая настройка (один раз):

1. Откройте файл `quick-update.bat` в блокноте
2. Найдите строку с `set SERVER=ВАШ_IP_АДРЕС_ЗДЕСЬ`
3. Замените на ваш IP адрес: `set SERVER=123.45.67.89`
4. Сохраните файл

### ⚡ Использование (после каждого изменения):

1. Откройте `quick-update.bat` двойным кликом
2. Дождитесь завершения (30 секунд)
3. Готово!

**Что делает скрипт:**
- ✅ Загружает файлы на сервер
- ✅ Перезапускает бота
- ✅ Показывает статус

---

## ✅ Способ 2: Через Git + GitHub (РЕКОМЕНДУЕТСЯ ДЛЯ ДОЛГОСРОЧНОГО ИСПОЛЬЗОВАНИЯ)

### 📝 Первая настройка (один раз):

#### 1. Создайте репозиторий на GitHub:
- Зайдите на https://github.com
- Нажмите "New repository"
- Назовите его (например) `Perps-info-tg-bot`
- НЕ добавляйте README, .gitignore, license

#### 2. Настройте Git на вашем компьютере:

Откройте PowerShell в папке проекта и выполните:

```powershell
# Инициализация Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Первый коммит
git commit -m "Initial commit"

# Подключите ваш репозиторий
git remote add origin https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git
git branch -M main
git push -u origin main
```

#### 3. Настройте на сервере:

Откройте файл `quick-update-git.bat` в блокноте и измените:
- `set SERVER=ВАШ_IP_АДРЕС_ЗДЕСЬ` → ваш IP
- `set GITHUB_REPO=https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git` → ссылка на ваш репозиторий
- Сохраните

Запустите один раз для настройки:
```cmd
quick-update-git.bat init
```

### ⚡ Использование (после каждого изменения):

1. Откройте `quick-update-git.bat` двойным кликом
2. Введите описание изменений (или просто нажмите Enter)
3. Дождитесь завершения
4. Готово!

**Преимущества:**
- ✅ История всех изменений
- ✅ Автоматическое обновление на сервере
- ✅ Легко откатить изменения
- ✅ Работает с несколькими компьютерами

---

## ✅ Способ 3: WinSCP (САМЫЙ ПРОСТОЙ ДЛЯ НОВИЧКОВ)

### 📝 Настройка (один раз):

1. Скачайте WinSCP: https://winscp.net/
2. Установите и откройте
3. Создайте новое подключение:
   - Протокол: SFTP
   - Host: ваш IP
   - Username: root
   - Password: ваш пароль
   - Сохраните

### ⚡ Использование:

1. Откройте WinSCP
2. Подключитесь к серверу
3. Откройте папку `/opt/Perps-info-tg-bot`
4. Перетащите измененные файлы (`bot.py`, `config.py`) из Windows в эту папку
5. Откройте терминал в WinSCP (кнопка "Терминал")
6. Выполните:
   ```bash
   systemctl restart perps-bot
   ```
7. Готово!

---

## ✅ Способ 4: Через SSH (РУЧНОЙ МЕТОД)

### ⚡ Использование:

1. Откройте PowerShell
2. Подключитесь к серверу:
   ```powershell
   ssh root@ваш_IP
   ```
3. Откройте файл для редактирования:
   ```bash
   nano /opt/Perps-info-tg-bot/bot.py
   ```
4. Внесите изменения в редакторе
5. Сохраните: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`
6. Перезапустите бота:
   ```bash
   systemctl restart perps-bot
   ```
7. Проверьте статус:
   ```bash
   systemctl status perps-bot
   ```

---

## 🔍 Проверка работы бота после обновления

### Способы проверки:

#### 1. Через SSH:
```bash
ssh root@ваш_IP "systemctl status perps-bot"
```

#### 2. В Telegram:
- Отправьте боту команду `/start`
- Проверьте, что всё работает

#### 3. Логи в реальном времени:
```bash
ssh root@ваш_IP "journalctl -u perps-bot -f"
```

---

## 📊 Сравнение способов

| Способ | Скорость | Сложность | Рекомендуется |
|--------|----------|-----------|---------------|
| **.bat файл** | ⚡ 30 сек | 🟢 Легко | ✅ Да |
| **Git + GitHub** | ⚡ 1 мин | 🟡 Средне | ✅ Да (лучше) |
| **WinSCP** | 🐢 3 мин | 🟢 Очень легко | ⚠️ Для новичков |
| **SSH вручную** | 🐢 5-10 мин | 🔴 Сложно | ❌ Не рекомендуется |

---

## 💡 Полезные команды

После любого обновления вы можете проверить статус:

```bash
# Подключитесь к серверу
ssh root@ваш_IP

# Проверьте статус
systemctl status perps-bot

# Посмотрите логи
journalctl -u perps-bot -f

# Перезапустите бота
systemctl restart perps-bot

# Остановите бота
systemctl stop perps-bot

# Запустите бота
systemctl start perps-bot
```

---

## ❓ Что делать если что-то пошло не так?

### Бот не запускается:

1. Посмотрите логи:
   ```bash
   ssh root@ваш_IP "journalctl -u perps-bot -n 50"
   ```

2. Проверьте .env файл:
   ```bash
   ssh root@ваш_IP "cat /opt/Perps-info-tg-bot/.env"
   ```

3. Проверьте правильность кода:
   ```bash
   ssh root@ваш_IP "python3 -m py_compile /opt/Perps-info-tg-bot/bot.py"
   ```

### Откатить изменения:

Если используете Git:
```bash
ssh root@ваш_IP "cd /opt/Perps-info-tg-bot && git log"
ssh root@ваш_IP "cd /opt/Perps-info-tg-bot && git reset --hard HEAD~1"
ssh root@ваш_IP "systemctl restart perps-bot"
```

---

## 🎯 Рекомендация

**Для начала:** Используйте `quick-update.bat` - это самый простой способ.

**Для долгосрочного использования:** Настройте Git + GitHub через `quick-update-git.bat` - это удобнее и надежнее.


