# Как быстро обновить бота на сервере после изменений

## 🎯 Рекомендуемый способ: Git + GitHub (самый удобный)

### Шаг 1: Инициализация Git (один раз)

```bash
# На вашем Windows компьютере
cd C:\Users\uri3l\Perps-info-tg-bot

# Инициализируйте Git (если еще не сделали)
git init

# Создайте файл .gitignore
echo __pycache__ > .gitignore
echo *.pyc >> .gitignore
echo .env >> .gitignore
echo *.log >> .gitignore

# Добавьте файлы
git add .
git commit -m "Initial commit"
```

### Шаг 2: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `Perps-info-tg-bot`)
3. НЕ добавляйте README, .gitignore и лицензию

### Шаг 3: Загрузите код на GitHub

```bash
# Подключите ваш репозиторий
git remote add origin https://github.com/ваш_username/Perps-info-tg-bot.git

# Загрузите код
git branch -M main
git push -u origin main
```

### Шаг 4: Настройте сервер для работы с Git

Подключитесь к серверу через SSH и выполните:

```bash
# Переход в папку проекта
cd /opt/Perps-info-tg-bot

# Если уже был клон
git pull origin main

# Если еще нет
git clone https://github.com/ваш_username/Perps-info-tg-bot.git /opt/Perps-info-tg-bot
```

## ⚡ Быстрое обновление (после каждого изменения)

### На вашем компьютере (Windows):

```bash
cd C:\Users\uri3l\Perps-info-tg-bot
git add .
git commit -m "Обновление описания проекта X"
git push
```

### На сервере:

```bash
ssh root@ваш_IP
cd /opt/Perps-info-tg-bot
git pull
systemctl restart perps-bot
```

**Готово!** Бот обновлен за 30 секунд! ⚡

---

## 🚀 Альтернатива 1: Batch скрипт для быстрой загрузки

### Создайте файл `deploy-update.bat`:

```batch
@echo off
set SERVER=ваш_IP_адрес
set PORT=22

echo ========================================
echo Быстрое обновление бота на сервере
echo ========================================
echo.

echo Загрузка файлов на сервер...
scp bot.py config.py requirements.txt root@%SERVER%:/opt/Perps-info-tg-bot/

echo.
echo Перезапуск бота на сервере...
ssh root@%SERVER% "systemctl restart perps-bot"

echo.
echo Проверка статуса...
ssh root@%SERVER% "systemctl status perps-bot"

echo.
echo ========================================
echo Бот успешно обновлен!
echo ========================================
pause
```

### Использование:

1. Откройте `deploy-update.bat` в блокноте
2. Замените `ваш_IP_адрес` на реальный IP сервера
3. Запустите `.bat` файл двойным кликом после каждого обновления

---

## 🎨 Альтернатива 2: WinSCP (самый простой)

### Быстрый процесс:

1. Откройте WinSCP
2. Подключитесь к серверу
3. В папке `/opt/Perps-info-tg-bot` найдите измененные файлы
4. Перетащите их из Windows на сервер (заменить)
5. В терминале WinSCP выполните:
   ```bash
   systemctl restart perps-bot
   ```

**Время: 2-3 минуты** ⏱️

---

## 🔄 Альтернатива 3: Скрипт-помощник для автоматического обновления

Используйте готовые скрипты для быстрого деплоя.

