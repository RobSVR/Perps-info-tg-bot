# 📚 Настройка GitHub для обновления бота

Пошаговая инструкция для работы с GitHub.

---

## 📋 Шаг 1: Создание репозитория на GitHub

### 1.1 Зарегистрируйтесь/Войдите на GitHub

1. Зайдите на https://github.com
2. Войдите в аккаунт (или создайте новый)

### 1.2 Создайте новый репозиторий

1. Нажмите кнопку **"+"** в правом верхнем углу
2. Выберите **"New repository"**
3. Заполните форму:
   - **Repository name**: `Perps-info-tg-bot`
   - **Description**: Описание бота (опционально)
   - **Public** или **Private** - на ваше усмотрение
   - **❌ НЕ ставьте галочки** на:
     - Add a README file
     - Add .gitignore
     - Choose a license
4. Нажмите **"Create repository"**

### 1.3 Скопируйте ссылку

После создания вы увидите ссылку:
```
https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git
```

**Скопируйте эту ссылку - она нам понадобится!**

---

## 📂 Шаг 2: Подготовка проекта на вашем компьютере

### 2.1 Откройте PowerShell в папке проекта

1. Зажмите `Shift` и правой кнопкой мыши на папке `Perps-info-tg-bot`
2. Выберите **"Открыть окно PowerShell здесь"**

Или откройте PowerShell и выполните:
```powershell
cd C:\Users\uri3l\Perps-info-tg-bot
```

### 2.2 Инициализируйте Git (если еще не сделано)

Проверьте, есть ли уже Git:
```powershell
git status
```

Если появилась ошибка "not a git repository", выполните:
```powershell
git init
```

### 2.3 Добавьте файлы в Git

```powershell
# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit - Perps Info Bot"
```

### 2.4 Подключите GitHub репозиторий

**ВАЖНО: Замените ссылку на вашу!**

```powershell
# Добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Пример:**
Если ваш username `ivan`, то:
```powershell
git remote add origin https://github.com/ivan/Perps-info-tg-bot.git
git push -u origin main
```

### 2.5 Проверьте

Зайдите на GitHub и убедитесь, что все файлы загружены:
```
https://github.com/ВАШ_USERNAME/Perps-info-tg-bot
```

---

## ⚙️ Шаг 3: Настройка .bat файла

### 3.1 Откройте `quick-update-git.bat` в блокноте

### 3.2 Измените настройки (строки 17-20)

Найдите эти строки:
```batch
set SERVER=ВАШ_IP_АДРЕС_ЗДЕСЬ
set USER=root
set GITHUB_REPO=https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git
```

Замените на ваши данные:

```batch
set SERVER=123.45.67.89                    ← IP вашего сервера
set USER=root
set GITHUB_REPO=https://github.com/ivan/Perps-info-tg-bot.git  ← ссылка на ваш репозиторий
```

### 3.3 Сохраните файл

---

## 🚀 Шаг 4: Первая настройка на сервере

### 4.1 Запустите команду инициализации

**Откройте PowerShell в папке проекта** и выполните:

```powershell
.\quick-update-git.bat init
```

Или просто запустите файл двойным кликом и введите `init` когда будет запрос.

### 4.2 Что произойдет

Скрипт выполнит:
- ✅ Загрузит код на GitHub (если нужно)
- ✅ Склонирует репозиторий на сервер
- ✅ Установит зависимости (pip install)
- ✅ Настроит systemd сервис
- ✅ Запустит бота

### 4.3 Проверьте работу

```powershell
ssh root@ваш_IP "systemctl status perps-bot"
```

Должен быть статус: `active (running)`

---

## ⚡ Шаг 5: Использование (обновление бота)

### 5.1 Внесите изменения в код

Например, измените `config.py` или `bot.py` в любом редакторе

### 5.2 Запустите обновление

**Просто запустите `quick-update-git.bat` двойным кликом!**

Скрипт попросит ввести описание изменений:
```
Введите описание обновления (или нажмите Enter для "Update"):
```

Введите что-то вроде:
- "Добавлен новый проект A"
- "Исправлена ошибка в описании"
- "Обновлена информация по Backpack"

Или просто нажмите `Enter` (будет "Update")

### 5.3 Готово!

Скрипт автоматически:
1. Добавит изменения в Git
2. Создаст коммит с вашим описанием
3. Отправит на GitHub
4. Обновит код на сервере
5. Перезапустит бота

**Всё это за 1 минуту!** ⚡

---

## 📊 Проверка работы

### Посмотреть историю изменений на GitHub:

Зайдите на: `https://github.com/ВАШ_USERNAME/Perps-info-tg-bot/commits/main`

Там будут все ваши обновления!

### Посмотреть статус бота:

```powershell
ssh root@ваш_IP "systemctl status perps-bot"
```

### Посмотреть логи:

```powershell
ssh root@ваш_IP "journalctl -u perps-bot -f"
```

---

## 🔄 Дополнительные возможности

### Откатить последнее обновление:

Если что-то пошло не так, можно откатить:

```powershell
ssh root@ваш_IP "cd /opt/Perps-info-tg-bot && git reset --hard HEAD~1"
ssh root@ваш_IP "systemctl restart perps-bot"
```

### Посмотреть историю изменений:

```powershell
ssh root@ваш_IP "cd /opt/Perps-info-tg-bot && git log --oneline -10"
```

### Обновить код вручную на сервере:

```powershell
ssh root@ваш_IP "cd /opt/Perps-info-tg-bot && git pull && systemctl restart perps-bot"
```

---

## ⚠️ Важные моменты

### 1. Файл .env НЕ загружается на GitHub

По соображениям безопасности ваш `.env` файл не попадет в GitHub благодаря `.gitignore`.

**Это правильно!** Всегда храните `.env` только на сервере.

### 2. Если забыли закоммитить изменения

Если вы запустили скрипт, но изменения не применились:

```powershell
git status
```

Посмотрите, какие файлы не закоммичены, и добавьте их вручную:

```powershell
git add .
git commit -m "Ваше описание"
git push
```

### 3. Конфликт изменений

Если на сервере кто-то изменил код напрямую, может возникнуть конфликт. Решите его:

```powershell
ssh root@ваш_IP "cd /opt/Perps-info-tg-bot && git stash && git pull && systemctl restart perps-bot"
```

---

## 🎉 Готово!

Теперь вы можете:
- ✅ Обновлять бота за 1 минуту
- ✅ Видеть историю всех изменений
- ✅ Легко откатывать ошибки
- ✅ Работать с ботом с нескольких компьютеров

**Всё готово к работе!** 🚀


