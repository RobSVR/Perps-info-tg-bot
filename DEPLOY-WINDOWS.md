# Инструкция по деплою бота на сервер (Windows)

## 📋 Что вам нужно для начала

1. **Сервер (VPS)** - если его нет, закажите один из вариантов:
   - Hetzner (€4-5/мес) - https://hetzner.com
   - DigitalOcean ($6/мес) - https://digitalocean.com
   - Timeweb (от ₽300/мес) - https://timeweb.com

2. **Доступ к серверу:**
   - IP адрес сервера
   - Логин (обычно `root`)
   - Пароль или SSH ключ

3. **Программное обеспечение на Windows:**
   - WinSCP или FileZilla (для загрузки файлов)
   - PuTTY (для подключения к серверу)
   - Или просто используйте встроенный SSH (если Windows 10/11)

## 🚀 Пошаговая инструкция для Windows

### Шаг 1: Закажите сервер

Перейдите на один из сайтов выше и закажите сервер:
- ОС: Ubuntu 20.04 или 22.04
- Минимум: 1 CPU, 1GB RAM, 20GB SSD

Сохраните:
- IP адрес сервера
- Пароль root

### Шаг 2: Установите WinSCP (рекомендуется)

1. Скачайте WinSCP: https://winscp.net
2. Установите программу

### Шаг 3: Подключитесь к серверу через WinSCP

1. Запустите WinSCP
2. Создайте новое соединение:
   - **Протокол:** SFTP
   - **Имя хоста:** ваш IP адрес
   - **Порт:** 22
   - **Имя пользователя:** root
   - **Пароль:** ваш пароль от сервера
3. Нажмите "Войти"

### Шаг 4: Загрузите файлы бота на сервер

1. В WinSCP вы увидите две панели:
   - Слева - ваш компьютер (Windows)
   - Справа - сервер (Linux)

2. Слева перейдите в папку: `C:\Users\uri3l\Perps-info-tg-bot`

3. Справа создайте папку на сервере: `/opt/Perps-info-tg-bot`

4. Загрузите файлы:
   - Выделите ВСЕ файлы слева (bot.py, config.py, requirements.txt и т.д.)
   - ПЕРЕТАЩИТЕ их справа в папку `/opt/Perps-info-tg-bot`
   - Подождите, пока загрузка завершится

### Шаг 5: Подключитесь к серверу через SSH

**Вариант А: Через WinSCP (удобный способ)**
1. В WinSCP нажмите кнопку "Терминал" (на панели инструментов)
2. Откроется окно терминала

**Вариант Б: Через командную строку Windows**
1. Откройте PowerShell или CMD
2. Введите: `ssh root@ваш_IP_адрес`
3. Нажмите Enter
4. Введите пароль при запросе

### Шаг 6: Установите Python и зависимости на сервере

В терминале выполните команды по одной:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python 3 и pip
apt install python3 python3-pip -y

# Переход в папку проекта
cd /opt/Perps-info-tg-bot

# Установка зависимостей
pip3 install -r requirements.txt
```

### Шаг 7: Создайте файл .env с токеном

```bash
# Создание файла .env
nano .env
```

В открывшемся редакторе вставьте:
```
BOT_TOKEN=ваш_токен_бота
CHANNEL_ID=@ваш_канал
CHANNEL_ID_NUMERIC=-1001234567890
```

Нажмите:
- `Ctrl + X` - для выхода
- `Y` - для подтверждения
- `Enter` - для сохранения

### Шаг 8: Установите автозапуск (systemd)

```bash
# Копирование сервисного файла
cp perps-bot.service /etc/systemd/system/

# Активация автозапуска
systemctl daemon-reload
systemctl enable perps-bot
systemctl start perps-bot

# Проверка статуса
systemctl status perps-bot
```

Если всё работает, вы увидите: `active (running)` зелёным цветом.

### Шаг 9: Проверьте работу бота

```bash
# Просмотр последних логов
journalctl -u perps-bot -n 50 --no-pager
```

Если нет ошибок - бот работает! ✅

## 🎛️ Управление ботом

Для управления ботом подключитесь к серверу через SSH и используйте:

```bash
# Статус бота
systemctl status perps-bot

# Перезапуск бота
systemctl restart perps-bot

# Остановка бота
systemctl stop perps-bot

# Запуск бота
systemctl start perps-bot

# Просмотр логов в реальном времени
journalctl -u perps-bot -f
```

## 🆘 Если что-то пошло не так

### Бот не запускается:

1. Проверьте логи:
   ```bash
   journalctl -u perps-bot -n 100
   ```

2. Проверьте, что .env файл содержит токен:
   ```bash
   cat /opt/Perps-info-tg-bot/.env
   ```

3. Попробуйте запустить бота вручную для теста:
   ```bash
   cd /opt/Perps-info-tg-bot
   python3 bot.py
   ```

### Проблемы с подключением:

- Проверьте, что открыт SSH порт (22) на сервере
- Убедитесь, что IP адрес правильный
- Проверьте правильность пароля

### Файлы не загружаются:

- Проверьте, что используете SFTP протокол
- Убедитесь, что на сервере достаточно места: `df -h`

## 📝 Альтернативный вариант: Через GitHub

Если вы не хотите загружать файлы через WinSCP:

1. Создайте репозиторий на GitHub
2. Загрузите туда файлы проекта
3. На сервере выполните:
   ```bash
   apt install git -y
   cd /opt
   git clone https://github.com/ваш_username/Perps-info-tg-bot
   cd Perps-info-tg-bot
   pip3 install -r requirements.txt
   ```

## ✅ Готово!

После выполнения всех шагов ваш бот будет работать 24/7 на сервере! 🎉

Для любого вопроса: создайте Issue или напишите в Telegram.

