# 🔑 Настройка SSH ключей через WinSCP

Подробная пошаговая инструкция.

---

## 📋 Шаг 1: Создание SSH ключа на вашем компьютере

### Вариант A: Через WinSCP (проще всего)

1. **Откройте WinSCP**
2. **Идите в**: `Options` → `Preferences` → вкладка `Authentication`
3. **Нажмите кнопку** `"Generate or Import Key"`
4. **Выберите** `"Generate New"`
5. **Тип ключа**: выберите `ED25519` (или `SSH-2 RSA`, если нет ED25519)
6. **Нажмите** `"Generate"`
7. **Сохраните ключи**:
   - `"Save public key"` → сохраните как `id_ed25519.pub` (например, на Рабочий стол)
   - `"Save private key"` → сохраните как `id_ed25519` (например, в `C:\Users\uri3l\.ssh\`)
8. **Закройте окно** генерации ключа
9. **В WinSCP** в поле `"Private key file"` укажите путь к сохраненному приватному ключу

### Вариант B: Через PowerShell (если предпочитаете)

Откройте PowerShell и выполните:
```powershell
ssh-keygen -t ed25519
```

Когда спросит:
- "Enter file" → нажмите `Enter` (по умолчанию `C:\Users\uri3l\.ssh\id_ed25519`)
- "Enter passphrase" → нажмите `Enter` дважды (без пароля)

Ключ будет создан в `C:\Users\uri3l\.ssh\id_ed25519`

---

## 🌐 Шаг 2: Подключение к серверу через WinSCP

1. **Откройте WinSCP**
2. **Создайте новое подключение:**
   - **Протокол**: `SFTP`
   - **Хост**: `45.132.19.34`
   - **Порт**: `22`
   - **Имя пользователя**: `root`
   - **Пароль**: введите ваш пароль
3. **Нажмите** `"Login"` (или `"Сохранить"` если хотите сохранить подключение)

---

## 📂 Шаг 3: Создание папки .ssh на сервере

1. В **правой панели WinSCP** (это сервер) перейдите в папку `/root`
2. **Создайте папку** `.ssh`:
   - Правый клик → `"Create Directory"` 
   - Имя: `.ssh`
   - Нажмите `OK`

---

## 📋 Шаг 4: Загрузка публичного ключа на сервер

1. В **левой панели WinSCP** (это ваш компьютер) найдите файл `id_ed25519.pub`
   - Если создавали через WinSCP - там где сохранили
   - Если через PowerShell - в `C:\Users\uri3l\.ssh\id_ed25519.pub`

2. **Перетащите** файл `id_ed25519.pub` из левой панели в правую панель в папку `/root/.ssh/`

---

## 📝 Шаг 5: Создание файла authorized_keys

1. В **правой панели** (сервер) откройте папку `/root/.ssh/`

2. **Создайте файл `authorized_keys`**:
   - Правый клик в папке `.ssh` → `"New"` → `"File"`
   - Имя: `authorized_keys`
   - Нажмите `OK`

3. **Откройте файл `authorized_keys`** для редактирования:
   - Двойной клик на `authorized_keys`
   - WinSCP откроет его в редакторе

4. **Откройте публичный ключ на вашем компьютере**:
   - В левой панели WinSCP найдите `id_ed25519.pub`
   - Двойной клик - откроется в блокноте
   - **Скопируйте ВСЁ содержимое** (весь текст из файла)

5. **Вставьте ключ в файл `authorized_keys`**:
   - Вернитесь в открытый файл `authorized_keys` в WinSCP
   - Вставьте скопированный текст (Ctrl+V)
   - **Сохраните** (Ctrl+S)
   - **Закройте** редактор

---

## 🔐 Шаг 6: Установка правильных прав доступа

1. В **правой панели** найдите файл `authorized_keys` в папке `/root/.ssh/`

2. **Правый клик** на `authorized_keys` → `"Properties"` (или `"Свойства"`)

3. **Установите права:**
   - Снимите все галочки кроме:
     - **Owner**: `Read`, `Write`
     - Итого: должно быть **600** (rw-------)
   - Нажмите `OK`

4. **Проверьте права папки `.ssh`**:
   - Правый клик на папке `.ssh` → `"Properties"`
   - Установите: **700** (rwx------)

---

## ✅ Шаг 7: Проверка работы без пароля

1. **Настройте подключение в WinSCP для использования ключа:**
   - В WinSCP: `Options` → `Preferences` → `Authentication`
   - В поле `"Private key file"` укажите путь к вашему приватному ключу:
     - Если через WinSCP: где сохраняли
     - Если через PowerShell: `C:\Users\uri3l\.ssh\id_ed25519`
   - **Снимите галочку** `"Password"`
   - Поставьте галочку `"Private key file"`
   - Нажмите `OK`

2. **Переподключитесь:**
   - Сохраните подключение (если не сохраняли)
   - Подключитесь заново
   - **Пароль больше не должен запрашиваться!** ✅

3. **Проверьте в PowerShell:**
   ```powershell
   ssh root@45.132.19.34
   ```
   Должно подключиться без пароля!

---

## 🚀 Шаг 8: Запуск quick-update-git.bat

Теперь всё настроено! Можете запускать:

```powershell
.\quick-update-git.bat init
```

Пароль больше не потребуется! 🎉

---

## ⚠️ Важные замечания

- **Публичный ключ** (`id_ed25519.pub`) - можно хранить где угодно
- **Приватный ключ** (`id_ed25519`) - НИКОГДА не передавайте никому!
- Файл `authorized_keys` должен быть на **одной строке** (не разрывать!)

---

## ❓ Если что-то не работает

### Проблема: Все равно просит пароль

**Решение:**
1. Проверьте права: `.ssh` = 700, `authorized_keys` = 600
2. Убедитесь, что ключ скопирован полностью в `authorized_keys`
3. Проверьте, что в `authorized_keys` нет лишних переводов строк

### Проблема: Не могу открыть файл для редактирования

**Решение:** 
1. Создайте файл на компьютере, вставьте ключ
2. Перетащите файл на сервер
3. Затем установите права

---

## 💡 Альтернатива: Использовать терминал WinSCP

В WinSCP есть встроенный терминал:

1. После подключения нажмите `"Commands"` → `"Open Terminal"` (или Ctrl+P)
2. В терминале выполните команды:
   ```bash
   mkdir -p ~/.ssh
   nano ~/.ssh/authorized_keys
   ```
3. Вставьте ключ (правый клик в терминале WinSCP обычно работает)
4. Сохраните: Ctrl+O, Enter, Ctrl+X
5. Установите права:
   ```bash
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```

