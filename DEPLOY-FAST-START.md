# ⚡ БЫСТРЫЙ СТАРТ: Обновление бота за 30 секунд

## 🎯 Самый простой способ (для новичков)

### Шаг 1: Настройка (один раз)

1. Откройте файл **`quick-update.bat`** в блокноте
2. Найдите строку: `set SERVER=ВАШ_IP_АДРЕС_ЗДЕСЬ`
3. Замените на ваш IP сервера (например: `set SERVER=123.45.67.89`)
4. Сохраните файл

### Шаг 2: Использование (после каждого изменения)

Просто **запустите `quick-update.bat` двойным кликом** и всё!

✅ Автоматически загрузит файлы  
✅ Автоматически перезапустит бота  
✅ Покажет статус  

**Время: 30 секунд**

---

## 🚀 Продвинутый способ (рекомендуется)

### Что это даёт:
- 📝 История всех изменений
- 🔄 Легко откатить ошибки
- 💻 Можно работать с нескольких компьютеров
- 🤖 Автоматическое обновление

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Назовите: `Perps-info-tg-bot`
4. НЕ добавляйте README, .gitignore, license
5. Скопируйте ссылку на репозиторий

### Шаг 2: Настройте Git

Откройте PowerShell в папке проекта:

```powershell
# Инициализация
git init
git add .
git commit -m "Initial commit"

# Подключите ваш репозиторий (замените ссылку)
git remote add origin https://github.com/ВАШ_USERNAME/Perps-info-tg-bot.git
git branch -M main
git push -u origin main
```

### Шаг 3: Настройте .bat файл

Откройте **`quick-update-git.bat`** в блокноте:

```batch
set SERVER=ваш_IP                  ← замените
set GITHUB_REPO=ваша_ссылка        ← замените
```

Сохраните.

### Шаг 4: Настройка на сервере (один раз)

Запустите:
```cmd
quick-update-git.bat init
```

### Шаг 5: Использование

Просто **запустите `quick-update-git.bat` двойным кликом** и всё!

---

## 📚 Подробные инструкции

- **`QUICK-UPDATE-GUIDE.md`** - полная инструкция по обновлению
- **`SERVICE_REQUIREMENTS.md`** - как арендовать сервер
- **`README.md`** - общая информация о боте

---

## ❓ Что делать если не работает?

### Проблема: Не загружаются файлы

**Решение:** Проверьте SSH подключение
```powershell
ssh root@ваш_IP
```

### Проблема: Бот не перезапускается

**Решение:** Проверьте статус
```bash
ssh root@ваш_IP "systemctl status perps-bot"
```

### Проблема: Не видно логов

**Решение:** Посмотрите логи
```bash
ssh root@ваш_IP "journalctl -u perps-bot -n 50"
```

---

## 💡 Полезные команды

```bash
# Статус бота
ssh root@IP "systemctl status perps-bot"

# Логи в реальном времени
ssh root@IP "journalctl -u perps-bot -f"

# Перезапуск
ssh root@IP "systemctl restart perps-bot"

# Остановка
ssh root@IP "systemctl stop perps-bot"

# Запуск
ssh root@IP "systemctl start perps-bot"
```

---

## 🎉 Готово!

Теперь вы можете обновлять бота за **30 секунд** одним кликом!


