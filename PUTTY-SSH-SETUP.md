# 🔑 Настройка SSH ключей через PuTTY

Подробная пошаговая инструкция.

---

## 📦 Шаг 1: Скачать и установить PuTTY

1. **Скачайте PuTTY:**
   - Зайдите на https://www.putty.org/
   - Или напрямую: https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html
   - Скачайте `putty-64bit-X.XX-installer.msi` (или 32-bit если нужна 32-разрядная версия)

2. **Установите PuTTY:**
   - Запустите установщик
   - Нажимайте "Next" везде (настройки по умолчанию подходят)
   - После установки у вас будет:
     - `PuTTY` - основная программа
     - `PuTTYgen` - генератор ключей
     - `PSCP` - копирование файлов

---

## 🔑 Шаг 2: Создание SSH ключа через PuTTYgen

1. **Откройте `PuTTYgen`** (Пуск → PuTTY → PuTTYgen)

2. **Настройки генерации ключа:**
   - **Type of key**: выберите `ED25519` (рекомендуется)
   - Если нет ED25519, выберите `RSA`
   - **Number of bits**: если RSA, поставьте `2048`

3. **Генерация ключа:**
   - Нажмите кнопку `"Generate"`
   - **Пошевелите мышкой** в окне (это ускоряет генерацию)
   - Подождите, пока ключ сгенерируется

4. **Сохранение ключей:**

   **Сначала сохраните ПУБЛИЧНЫЙ ключ:**
   - В поле `"Key comment"` можете написать что-то вроде `"Perps Bot Server"`
   - Скопируйте текст из поля `"Public key for pasting into OpenSSH authorized_keys file"`
     - Выделите весь текст (Ctrl+A)
     - Скопируйте (Ctrl+C)
     - Это ваш публичный ключ!
   - **Сохраните публичный ключ как файл:**
     - Кнопка `"Save public key"`
     - Сохраните как `id_ed25519.pub` (например, на Рабочий стол)

   **Теперь сохраните ПРИВАТНЫЙ ключ:**
   - Кнопка `"Save private key"`
   - Сохраните как `id_ed25519.ppk` (например, в `C:\Users\uri3l\.ssh\`)
   - **ВНИМАНИЕ:** Если спросит подтверждение о сохранении без пароля - нажмите `"Yes"`
   - ⚠️ **ХРАНИТЕ ЭТОТ ФАЙЛ В БЕЗОПАСНОСТИ!** Это ваш ключ доступа

5. **Закройте PuTTYgen**

---

## 🌐 Шаг 3: Подключение к серверу через PuTTY

1. **Откройте PuTTY**

2. **Настройте подключение:**
   - **Host Name**: `root@45.132.19.34`
   - **Port**: `22`
   - **Connection type**: `SSH`

3. **Настройте автологин** (опционально, но удобно):
   - В левом меню: `Connection` → `Data`
   - `Auto-login username`: `root`

4. **Сохраните подключение:**
   - Вернитесь в `Session`
   - `Saved Sessions`: напишите `Perps Server` (или любое имя)
   - Нажмите `"Save"`

5. **Подключитесь:**
   - Нажмите `"Open"`
   - Если появится предупреждение о безопасности - нажмите `"Yes"`
   - Введите пароль (в PuTTY пароль НЕ отображается - это нормально!)
   - Нажмите Enter
   - Теперь вы подключены! ✅

---

## 📋 Шаг 4: Добавление ключа на сервер

1. **Вы уже подключены через PuTTY** (из шага 3)

2. **Создайте папку `.ssh`:**
   ```bash
   mkdir -p ~/.ssh
   ```

3. **Создайте файл `authorized_keys`:**
   ```bash
   nano ~/.ssh/authorized_keys
   ```

4. **Вставьте публичный ключ:**
   - **Правый клик мыши** в окне PuTTY - вставит скопированный ключ
   - Или используйте кнопку вверху окна PuTTY для вставки
   - Ключ должен быть на **одной строке** (без разрывов)

5. **Сохраните файл в nano:**
   - `Ctrl+O` (сохранить)
   - `Enter` (подтвердить имя файла)
   - `Ctrl+X` (выйти)

6. **Установите правильные права:**
   ```bash
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```

7. **Проверьте:**
   ```bash
   cat ~/.ssh/authorized_keys
   ```
   Должен показать ваш ключ (начинается с `ssh-ed25519`)

8. **Отключитесь:**
   ```bash
   exit
   ```

---

## ⚙️ Шаг 5: Настройка PuTTY для использования ключа

1. **Откройте PuTTY**

2. **Загрузите сохраненное подключение:**
   - В списке `Saved Sessions` выберите `"Perps Server"`
   - Нажмите `"Load"`

3. **Настройте ключ:**
   - В левом меню: `Connection` → `SSH` → `Auth` → `Credentials`
   - Нажмите кнопку `"Browse..."` рядом с `"Private key file for authentication"`
   - Найдите и выберите ваш файл `id_ed25519.ppk` (приватный ключ)
   - Ключ загружен!

4. **НЕ ИСПОЛЬЗУЙТЕ пароль:**
   - В `Connection` → `Data`
   - Убедитесь, что `"Auto-login username"` = `root`
   - Пароль НЕ нужен, если ключ настроен правильно

5. **Сохраните настройки:**
   - Вернитесь в `Session`
   - Нажмите `"Save"`

6. **Подключитесь:**
   - Нажмите `"Open"`
   - **Пароль больше НЕ должен запрашиваться!** ✅

---

## ✅ Шаг 6: Проверка работы

1. **Через PuTTY:**
   - Подключитесь заново
   - Должно подключиться без пароля!

2. **Через PowerShell (опционально):**
   ```powershell
   ssh root@45.132.19.34
   ```
   Должно подключиться без пароля!

---

## 🚀 Шаг 7: Использование скриптов

Теперь можете использовать скрипты:

```powershell
.\quick-update-git.bat init
```

Пароль больше не нужен! 🎉

---

## ⚠️ Важные замечания

### Формат ключей:
- **PuTTY ключ**: `.ppk` файл (только для PuTTY)
- **OpenSSH ключ**: без расширения или `.pub` (для ssh команды)

### Если хотите использовать `ssh` команду в PowerShell:
Нужно конвертировать `.ppk` в OpenSSH формат:

1. Откройте `PuTTYgen`
2. `Conversions` → `Import key`
3. Выберите ваш `id_ed25519.ppk`
4. `Conversions` → `Export OpenSSH key`
5. Сохраните как `id_ed25519` (без расширения)
6. Положите в `C:\Users\uri3l\.ssh\`

---

## 🎯 Быстрая справка PuTTY

### Горячие клавиши:
- **Вставка текста**: Правый клик мыши (или кнопка вверху окна)
- **Копирование**: Выделите текст (он автоматически копируется)
- **Сохранить сессию**: `Alt+Enter`

### Полезные настройки:
- **Автоматическая переподключение**: `Connection` → `SSH` → `"Enable keepalives"`
- **Увеличить размер шрифта**: `Window` → `Appearance` → измените шрифт
- **Сохранить логи сессии**: `Session` → `Logging` → включите логирование

---

## ❓ Решение проблем

### Проблема: "Server refused our key"

**Решение:**
1. Проверьте права: `.ssh` = 700, `authorized_keys` = 600
2. Убедитесь, что ключ вставлен полностью в `authorized_keys`
3. Проверьте формат ключа в `authorized_keys` (должен быть OpenSSH формат)

### Проблема: Все равно просит пароль

**Решение:**
1. Проверьте, что выбрали правильный `.ppk` файл в PuTTY
2. Убедитесь, что сохранили настройки подключения
3. Попробуйте подключиться заново после перезапуска PuTTY

### Проблема: Не могу вставить ключ в nano

**Решение:**
1. В PuTTY правый клик обычно работает
2. Или используйте кнопку вверху окна PuTTY для вставки
3. Проверьте, что скопировали ключ ПОЛНОСТЬЮ (включая `ssh-ed25519` в начале)

---

## 📝 Альтернатива: Использовать WinSCP + PuTTY

Если вам удобнее работать через WinSCP:

1. Подключитесь через WinSCP с паролем
2. В WinSCP: `Session` → `Open in PuTTY`
3. PuTTY откроется уже подключенным
4. Выполните команды для настройки ключа (из Шага 4)

---

Готово! Теперь SSH ключи настроены через PuTTY! 🎉

